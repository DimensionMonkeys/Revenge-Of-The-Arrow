using UnityEngine;
using UnityEngine.UI;

public class ShopRechargeView : View
{
    //[SerializeField] private Button backBtn;

    public override void Initialize()
    {
        //backBtn.onClick.AddListener(() => ViewManager.showBeforeLast());
    }
}
